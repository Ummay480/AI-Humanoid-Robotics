# Core Dependencies for AI-Robot Brain (NVIDIA Isaac™)
# Python 3.11+ required

# Data validation and serialization
pydantic>=2.5.0,<3.0.0

# YAML parsing for skill definitions and configuration
pyyaml>=6.0,<7.0

# Core AI/ML libraries
numpy>=1.24.0
opencv-python>=4.8.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
pycuda>=2022.1
nvidia-ml-py3>=11.495.0

# ROS 2 integration
rclpy>=3.3.0

# Navigation and mapping
navigation2>=1.1.0

# Utilities
requests>=2.28.0
pytest>=7.2.0
pytest-asyncio>=0.20.0

# Async/await support (built-in but specified for clarity)
# asyncio is built-in to Python 3.11+

# Development Dependencies (install with: pip install -r requirements-dev.txt)
